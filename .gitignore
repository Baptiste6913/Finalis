# Runtime state: the document store, uploaded PDFs, the CLI scratch folder, the learning state.
data/
# Python and editor noise
__pycache__/
*.pyc
.DS_Store
.env
# Simulation run logs and intermediate result snapshots
sim/log_*.txt
sim/results_run*.json
# Screenshots written by the tests
tests/shot_*.png
tests/hl_*.png
# Publish copies of the artifact build (identical to dist/, regenerated by build.py)
/prescreen.html
/prescreen-demo.html
