# Test dependencies (the app itself needs only Python 3.9+ and, for the AI, Claude Code or an API key)
playwright>=1.40
python-pptx>=0.6.21
python-docx>=1.1
# system tools used by test_pdfedit.py and test_office.py: qpdf, poppler-utils (pdftotext, pdftoppm), libreoffice (soffice)
